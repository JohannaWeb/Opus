{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for the Opus prototype.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
